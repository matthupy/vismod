/vismod
/vismod.exe
*.exe~
/dist/
*.test
coverage.out
/audit.log
/results.jsonl
.env

# docker compose local stack: secrets and the per-operator config copy
deploy/compose/config.compose.yaml
# docker compose production example: its config/media mounts resolve
# against deploy/compose/ (compose's project directory), not the repo root.
deploy/compose/config.yaml
media/
!media/.gitkeep
