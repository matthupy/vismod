# Binaries (anchored to repo root — a bare `vismod` also matches the
# cmd/vismod/ source dir and silently un-tracks the entrypoint).
/vismod
/vismod.exe
/dist/

# Local artifacts
*.out
*.err
audit.log
*.tmp

# Docker-compose secrets + mounted media (.env.example IS tracked)
.env
/data/

# Test/sample media — NEVER commit media (esp. potential illegal content)
/testmedia/
sample.*

# Go
/vendor/
*.test
coverage.out
coverage.txt

# Editor / OS
.idea/
.vscode/
.DS_Store
Thumbs.db

# Local agent config
/.claude/
/.handoff/
